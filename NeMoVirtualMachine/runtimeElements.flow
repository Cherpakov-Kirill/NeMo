import ds/tree;
export {
    Value ::= exInt, exBool, exArr;
    exArr : (array : [Value]);
    exInt(value: int);
    exBool(value: bool);
    
    State : (label : int, vars : Tree<string, Value>);
}