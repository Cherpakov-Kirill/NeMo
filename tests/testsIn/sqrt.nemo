VAR n : INT;
VAR x : INT;
VAR y : INT;
(
    n := 121;
    y := 0;
    x := 1;
    while (y <= n){
        (
            y := (y + (x + (x + 1)));
		    x := (x + 1)
        )
    };
    x := (x - 1);
    print(x)
)