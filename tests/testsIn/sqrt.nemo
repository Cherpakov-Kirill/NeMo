VAR input: INT;
VAR degree: INT;
VAR delta: INT;
(
    input := 121;
    degree := 0;
    delta := 1;
    while (input >= delta){
        (
            input := (input - delta);
            delta := (delta + 2);
            degree := (degree + 1)
        )
    };
    print(degree)
)