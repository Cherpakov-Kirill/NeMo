import runtime;
import NeMoVirtualMachine/nemovm;

launchTest(filename : string){
    fileIn = "./tests/testsIn/" + filename;
    println("TestFile: " + fileIn);
    testCode = getFileContent(fileIn);
    println(testCode);
    switch (vmstr2vmprogram(testCode) : Maybe<VMProgram>) {
        Some(prog):{
            println("vmstr2vmprogram(fileContent : string)");
            println(prog);
            /*println("");
            println("program2str(NeMoProg : Program)");
            progStr = program2str(prog);
            println(progStr);
            println("");
            if(setFileContent("./tests/testsOut/" + "program2str_"+filename, progStr)){
                println("File was created");
            }
            else{
                println("File was not created");
            }
            println("");
            switch (str2program(progStr) : Maybe<Program>) {
                Some(prog2):{
                    println("str2program(programFromNeMo2str)");
                    println(prog2);
                    println("");
                    if(prog == prog2) {
                        println("Seccessful: NeMo -> string -> NeMo");
                    }
                    else {
                        println("Error: NeMo -> string -> NeMo");
                    }
                }
                None():{
                    println("ParserError: " + filename + " -> NeMo -> string -> NeMo");     
                }
            }*/
        }
        None():{
            println("ParserError: " + filename + " -> VMNeMo");     
        }
    }
}

main(){
    //launchTest("test1.vmnemo");
    launchTest("test2.vmnemo");
    //launchTest("test3.vmnemo");
    //launchTest("test4.vmnemo");
    //launchTest("test5.vmnemo");
}